# This workflow will build a docker container, publish it to IBM Container Registry, and deploy it to IKS when there is a push to the "main" branch.
#
# To configure this workflow:
#
# 1. Ensure that your repository contains a Dockerfile
# 2. Setup secrets in your repository by going to settings: Create ICR_NAMESPACE and IBM_CLOUD_API_KEY
# 3. Change the values for the IBM_CLOUD_REGION, REGISTRY_HOSTNAME, IMAGE_NAME, IKS_CLUSTER, DEPLOYMENT_NAME, and PORT

name: Build and Deploy to Code Engine

on:
  workflow_dispatch:
    branches: [ main ]
  push:
    branches: [ "main" ]

# Environment variables available to all jobs and steps in this workflow
env:
  GITHUB_SHA: ${{ github.sha }}
  IBMCLOUD_API_KEY: ${{ secrets.IBMCLOUD_API_KEY }}
  IBMCLOUD_REGION: ${{ vars.IBMCLOUD_REGION }}
  ICR_NAMESPACE: ${{ vars.ICR_NAMESPACE }}
  RESOURCE_GROUP: ${{ vars.RESOURCE_GROUP }}
  REGISTRY_HOSTNAME: ${{ vars.ICR_REGION }}
  IMAGE_NAME: ${{ vars.IMAGE_NAME }}
  PROJECT_NAME: ${{ vars.PROJECT_NAME }}
  APP_NAME: ${{ vars.APP_NAME }}

jobs:

  ci-pipeline:
    name: CI Pipeline
    runs-on: ubuntu-latest
    environment: production
    steps:

    - id: checkout-ci-code
      name: Checkout CI code
      uses: actions/checkout@v4

    - id: detect-secrets
      name: Detect CI code secrets
      run: | 
        echo "Detect CI code secrets <todo>"
  
    - id: unit-test-ci-code
      name: Unit test CI code
      run: |
        npm install
        npm run test-unit

    - id: build-ci-image
      name: Build CI image using Code Engine
      uses: IBM/code-engine-github-action@v1
      with:
        api-key: ${{ secrets.IBMCLOUD_API_KEY }}
        resource-group: ${{ vars.RESOURCE_GROUP }}
        region: ${{ vars.IBMCLOUD_REGION }}
        project: ${{ vars.PROJECT_NAME }}
        component: 'build'
        name: 'build-ci-image'
        image: "private.${{ vars.ICR_REGION }}/${{ vars.ICR_NAMESPACE }}/ci-${{ vars.IMAGE_NAME }}:latest"
        registry-secret: "ce-auto-icr-private-${{ vars.IBMCLOUD_REGION }}"
        build-source: './'

    - id: scan-icr-ci-image
      name: Scan ICR CI image
      run: |
        echo "Scan ICR CI image with digest: ${{ steps.build-ci-image.outputs.image-with-digest }} <todo>"
    
    - id: deploy-ci-app-to-code-engine
      name: Deploy CI application to Code Engine
      uses: IBM/code-engine-github-action@v1
      with:
        api-key: ${{ secrets.IBMCLOUD_API_KEY }}
        resource-group: ${{ vars.RESOURCE_GROUP }}
        region: ${{ vars.IBMCLOUD_REGION }}
        project: ${{ vars.PROJECT_NAME }}
        name: ${{ vars.APP_NAME }}
        component: 'app'
        image: "private.${{ vars.ICR_REGION }}/${{ vars.ICR_NAMESPACE }}/ci-${{ vars.IMAGE_NAME }}:latest"
        cpu: 1
        memory: 4G
